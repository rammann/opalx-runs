/*  noop_flag.in -- generated by make_decks.py (collimator validation suite).
    DELETEONTRANSVERSEEXIT=FALSE: the collimator must delete nothing.
    APERTURE = CIRCLE(0.02)   DELETEONTRANSVERSEEXIT = False
    Straight line, no fields, no space charge: transverse coordinates are
    invariant, so the survivor set is the grid points inside the aperture.
*/

OPTION, PSDUMPFREQ   = 500;
OPTION, STATDUMPFREQ = 5;
OPTION, BOUNDPDESTROY = 1000000;
Option, VERSION = 10900;

Title, string="noop_flag";

REAL Edes  = 0.1;
REAL gamma = (Edes + EMASS) / EMASS;
REAL beta  = sqrt(1 - (1 / gamma^2));
REAL P0    = gamma * beta * EMASS;          // GeV/c; also sets OpalData P0 (field ref)

D_IN:  DRIFT, L = 0.5, ELEMEDGE = 0.0;
COLL:  COLLIMATOR, L = 0.1, ELEMEDGE = 0.5,
       APERTURE = "CIRCLE(0.02)",
       DELETEONTRANSVERSEEXIT = FALSE;
D_OUT: DRIFT, L = 0.9, ELEMEDGE = 0.6;

CollLine: Line = (D_IN, COLL, D_OUT);

FS1: Fieldsolver, TYPE = NONE,
     NX = 16, NY = 16, NZ = 16,
     PARFFTX = true, PARFFTY = true, PARFFTZ = true,
     BCFFTX = open, BCFFTY = open, BCFFTZ = open,
     BBOXINCR = 1, GREENSF = INTEGRATED;

REAL n_particles = 57;
Dist: DISTRIBUTION, TYPE = FROMFILE, FNAME = "parts.txt", NPARTDIST = n_particles;
ES1: EMISSIONSOURCE, DISTRIBUTION = Dist;
mySources: EMISSIONSOURCELIST = (ES1);
BEAM1: BEAM, PARTICLE = ELECTRON, NALLOC = n_particles,
       BCHARGE = 1.6e-15, SOURCES = mySources, CHARGE = -1;

TRACK, LINE = CollLine, BEAM = BEAM1,
       MAXSTEPS = 2000000, DT = 1e-12, ZSTOP = 1.2;
RUN, METHOD = "PARALLEL", FIELDSOLVER = FS1;
ENDTRACK;
Quit;
